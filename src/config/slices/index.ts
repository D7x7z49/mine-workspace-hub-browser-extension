import { ConfigStore } from './interfaces';
import { createThemeSlice } from './ThemeSlice';
import { createLayoutSlice } from './LayoutSlice';
import { createSharedSlice } from './SharedSlice';
import { createGlobalSlice } from './GlobalSlice';
import { createWorkspaceSlice } from './WorkspaceSlice';

export { ConfigStore, createThemeSlice, createLayoutSlice, createWorkspaceSlice, createSharedSlice, createGlobalSlice };
